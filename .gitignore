# Xcode
DerivedData/
build/
*.xcodeproj/
*.xcworkspace/
xcuserdata/
*.xcuserstate
*.moved-aside

# xcodegen-generated
FMail/Info.plist

# Swift Package Manager
.swiftpm/
.build/
Packages/
Package.resolved

# macOS
.DS_Store

# Google Cloud Console OAuth downloads. The Gmail writeback feature that used
# these is gone; the pattern stays so a stray download can't be committed.
client_secret_*.json

# Local prefs
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

# Claude Code worktrees
.claude/worktrees/
